# CI workflow for backend build and tests
name: Backend CI

on:
  # Trigger on pushes to develop or feature branches
  push:
    branches: [ "develop", "feature/*" ]
  # Trigger on pull requests to develop or main
  pull_request:
    branches: [ "develop", "main" ]
  workflow_dispatch:

jobs:
  build-test:
    runs-on: ubuntu-latest

    services:
      # MySQL database for integration tests
      db:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: ${{ secrets.MYSQL_ROOT_PASSWORD }}
          MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
          MYSQL_USER: ${{ secrets.MYSQL_USER }}
          MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
          MYSQL_ALLOW_EMPTY_PASSWORD: "no"
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      # Get the code from the repository
      - name: Checkout code
        uses: actions/checkout@v4

      # Install JDK 21 for running Spring Boot
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      # Copy SQL init script for database setup
      - name: Copy init.sql to MySQL init directory
        run: |
          mkdir -p ./mysql-init
          cp docker-entrypoint-initdb.d/init.sql ./mysql-init/init.sql

      # Wait for MySQL to start and run schema init
      - name: Initialize Database Schema
        run: |
          echo "Waiting for MySQL to be healthy..."
          for i in {1..30}; do
            if mysqladmin ping -h 127.0.0.1 -u root -p"${{ secrets.MYSQL_ROOT_PASSWORD }}" --silent; then
              echo "MySQL is up! Running init.sql..."
              cat ./mysql-init/init.sql | mysql -h 127.0.0.1 -u root -p"${{ secrets.MYSQL_ROOT_PASSWORD }}" ${{ secrets.MYSQL_DATABASE }}
              echo "Database initialized!"
              exit 0
            fi
            echo "Waiting for MySQL... ($i/30)"
            sleep 5
          done
          echo "MySQL failed to start."
          exit 1

      # Check if tables were created correctly
      - name: Verify tables created
        run: |
          echo "Listing tables in ${{ secrets.MYSQL_DATABASE }}..."
          echo "SHOW TABLES;" | mysql -h 127.0.0.1 -u ${{ secrets.MYSQL_USER }} -p"${{ secrets.MYSQL_PASSWORD }}" ${{ secrets.MYSQL_DATABASE }}

      # Give Maven wrapper execute permissions
      - name: Ensure mvnw is executable
        working-directory: ./backend
        run: chmod +x ./mvnw

      # Run all backend tests
      - name: Run unit & integration tests
        working-directory: ./backend
        env:
          SPRING_DATASOURCE_URL: jdbc:mysql://127.0.0.1:3306/${{ secrets.MYSQL_DATABASE }}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Amsterdam
          SPRING_DATASOURCE_USERNAME: ${{ secrets.MYSQL_USER }}
          SPRING_DATASOURCE_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
          SPRING_JPA_HIBERNATE_DDL_AUTO: validate
        run: ./mvnw clean test -Dspring.profiles.active=test

      # Upload test result reports as artifact
      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: ./backend/target/surefire-reports/
