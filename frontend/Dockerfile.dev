# Development image based on Node 20
FROM node:20
WORKDIR /app

# Copy package files first for dependency caching
COPY package*.json ./
RUN npm ci

# Copy source code (overwritten by bind mount in development)
COPY . .

# Start Vite dev server accessible externally
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
EXPOSE 5173